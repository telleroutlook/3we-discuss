{
  "name": "3we-discuss",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev --env production --port 8787 --local",
    "dev:full": "wrangler dev --env production --port 8787 --local & WRANGLER_PID=$!; trap 'kill $WRANGLER_PID 2>/dev/null' EXIT; vite --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "type-check": "tsc --noEmit",
    "db:local:init": "wrangler d1 execute 3we-discuss-db --local --file sql/001_init.sql && wrangler d1 execute 3we-discuss-db --local --file sql/002_seed_categories.sql",
    "db:remote:init": "wrangler d1 execute 3we-discuss-db --remote --file sql/001_init.sql && wrangler d1 execute 3we-discuss-db --remote --file sql/002_seed_categories.sql",
    "deploy": "npm run build && wrangler deploy --env production"
  },
  "dependencies": {
    "@solidjs/router": "^0.15.3",
    "lucide-solid": "^0.561.0",
    "marked": "^15.0.0",
    "solid-js": "^1.9.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.1",
    "@tailwindcss/postcss": "^4.1.0",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0",
    "vite-plugin-solid": "^2.10.2",
    "wrangler": "^4.56.0"
  }
}
